% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrtstack-mdim.R
\name{mdim_template_extend}
\alias{mdim_template_extend}
\title{Extend an mdim VRT recipe to a full file set}
\usage{
mdim_template_extend(xml, files, concat_values = NULL, concat_dim = "time")
}
\arguments{
\item{xml}{Template VRT as an XML string (or anything \code{\link[xml2:read_xml]{xml2::read_xml()}} takes).}

\item{files}{Full character vector of sources.}

\item{concat_values}{Values for the concat coordinate array, or \code{NULL} to
keep the template's values (only valid when length matches \code{files}).}

\item{concat_dim}{Concat dimension name (default \code{"time"}); if not found,
the dimension whose \code{size} equals the template's per-array source count is
used.}
}
\value{
An \code{xml_document} (serialise with \code{\link[=as.character]{as.character()}} / \code{\link[xml2:write_xml]{xml2::write_xml()}}).
}
\description{
Pure XML-tree rewrite (no GDAL needed). Sets the concat dimension size,
rewrites the concat coordinate array's values, and regenerates the
\verb{<Source>} list of every \emph{data} array (an \verb{<Array>} that owns a \verb{<Source>}).
Per-array \code{SourceArray} path and slab geometry are read from the array's
first source, never assumed from the array name.
}
