% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_mdim_stack.R
\name{gdal_mdim_stack}
\alias{gdal_mdim_stack}
\title{Create a Multidimensional VRT from 2D Rasters}
\usage{
gdal_mdim_stack(
  files,
  dim_name,
  dim_values = NULL,
  dim_start = NULL,
  dim_increment = NULL,
  dim_type = NULL,
  dim_direction = NULL,
  dim_unit = NULL,
  time_origin = NULL,
  time_unit = "days",
  parse_format = NULL,
  array_name = NULL,
  output = NULL
)
}
\arguments{
\item{files}{Character vector of file paths (COGs, GeoTIFFs, /vsicurl/ URIs, etc.)}

\item{dim_name}{Name for the new dimension (e.g., "time", "depth", "level")}

\item{dim_values}{Dimension values. One of:
\itemize{
\item Numeric vector of values (length must match files)
\item A regex pattern with one capturing group to extract from filenames
\item A function taking filename (basename) and returning a numeric value
\item NULL if using \code{dim_start} + \code{dim_increment}
}}

\item{dim_start, dim_increment}{For regular axes, specify start and increment
instead of explicit values. Files are assumed to be in the correct order.}

\item{dim_type}{Optional dimension type: \code{"TEMPORAL"}, \code{"VERTICAL"}, \code{"PARAMETRIC"}}

\item{dim_direction}{Optional direction: \code{"NORTH"}, \code{"SOUTH"}, \code{"EAST"}, \code{"WEST"},
\code{"UP"}, \code{"DOWN"}, \code{"FUTURE"}, \code{"PAST"}}

\item{dim_unit}{Unit string for the dimension (e.g., \code{"days since 1970-01-01"}, \code{"cm"})}

\item{time_origin}{For temporal dimensions, origin date as \code{"YYYY-MM-DD"} or POSIXct.
When set with \code{parse_format}, values are encoded as offsets from this origin.}

\item{time_unit}{For temporal dimensions: \code{"seconds"}, \code{"minutes"}, \code{"hours"}, \code{"days"}}

\item{parse_format}{For date/time extraction, a \code{\link[=strptime]{strptime()}} format string
(e.g., \code{"\%Y\%m\%d"} for \code{"20230115"})}

\item{array_name}{Name for the data array in the VRT. Default: derived from
common filename prefix, or \code{"data"}.}

\item{output}{Output VRT path. If NULL, returns XML string without writing.}
}
\value{
Path to VRT file (invisibly), or XML string if \code{output} is NULL.
}
\description{
Stack 2D rasters along a new dimension, producing a GDAL Multidimensional VRT.
This enables treating a collection of 2D rasters (COGs, GeoTIFFs, etc.) as a
single multidimensional array, readable by GDAL's multidim API and xarray backends.
}
\details{
The function generates a GDAL Multidimensional VRT that virtually stacks
2D rasters along a new dimension. Key features:
\itemize{
\item \strong{Regular axes}: When dimension values are evenly spaced (or specified via
\code{dim_start}/\code{dim_increment}), the VRT uses \verb{<RegularlySpacedValues>} for
compact representation.
\item \strong{Auto-detection}: Even with explicit \code{dim_values}, regular spacing is
detected automatically and converted to the compact form.
\item \strong{Filename parsing}: Extract dimension values from filenames using regex,
with optional date/time parsing. Useful for time series like
\code{"temperature_20230101.tif"}.
\item \strong{Cloud-native}: Supports \verb{/vsicurl/} URIs for remote files. HTTP(S) URLs
are automatically prefixed with \verb{/vsicurl/}.
}

The resulting VRT can be read by:
\itemize{
\item GDAL's multidim API: \code{gdalmdiminfo}, \code{gdalmdimtranslate}
\item Python xarray backends: gdx, xgdal
\item Any tool supporting GDAL multidimensional rasters
}
}
\examples{
\dontrun{
# Stack soil layers by depth
gdal_mdim_stack(
  files = c("soil_0cm.tif", "soil_30cm.tif", "soil_100cm.tif"),
  dim_name = "depth",
  dim_values = c(0, 30, 100),
  dim_type = "VERTICAL",
  dim_direction = "DOWN",
  dim_unit = "cm",
  output = "soil_stack.vrt"
)

# Time series with regex extraction
gdal_mdim_stack(
  files = list.files(pattern = "temp_\\\\d{8}\\\\.tif$"),
  dim_name = "time",
  dim_values = "(\\\\d{8})",
  parse_format = "\%Y\%m\%d",
  dim_type = "TEMPORAL",
  time_origin = "1970-01-01",
  time_unit = "days",
  output = "temperature.vrt"
)

# Regular axis - monthly data
gdal_mdim_stack(
  files = sprintf("precip_\%02d.tif", 1:12),
  dim_name = "month",
  dim_start = 1,
  dim_increment = 1,
  output = "monthly.vrt"
)

# Cloud-hosted COGs
gdal_mdim_stack(
  files = paste0(
    "https://storage.googleapis.com/solus100pub/claytotal_",
    c(0, 5, 15, 30, 60, 100, 150), "_cm_p.tif"
  ),
  dim_name = "depth",
  dim_values = c(0, 5, 15, 30, 60, 100, 150),
  dim_type = "VERTICAL",
  dim_unit = "cm",
  array_name = "claytotal",
  output = "solus_clay.vrt"
)
}
}
