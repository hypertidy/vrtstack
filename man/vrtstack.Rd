% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrtstack-mdim.R
\name{vrtstack}
\alias{vrtstack}
\title{Stack rasters into a multidimensional VRT (two paths, one entry point)}
\usage{
vrtstack(
  files,
  concat = NULL,
  template = NULL,
  concat_dim = "time",
  n_template = 10,
  parse_format = NULL,
  origin = NULL,
  unit = "days",
  output = NULL,
  ...
)
}
\arguments{
\item{files}{Character vector of source paths/URIs (full set).}

\item{concat}{Concat-axis spec, resolved by \code{\link[=resolve_concat]{resolve_concat()}}: a numeric/
character vector (length == \code{files}), a single regex with one capture
group to pull from basenames, a function of basename, or \code{NULL} to use
\code{concat_start}/\code{concat_increment} (from-scratch) or the template's own
values (template-extend, when concat is \code{NULL}).}

\item{template}{One of: \code{NULL} (from-scratch); a path to a \code{.vrt}; an XML
string; or \code{TRUE} to auto-generate the recipe from \code{head(files, n_template)}
via \verb{gdal mdim mosaic}.}

\item{concat_dim}{Name of the concatenation dimension. Default \code{"time"}.}

\item{n_template}{Files used to build the auto-generated recipe when
\code{template = TRUE}. Default 10.}

\item{parse_format, origin, unit}{Passed to \code{\link[=resolve_concat]{resolve_concat()}} for date parsing.}

\item{output}{Output VRT path, or \code{NULL} to return the XML string.}

\item{...}{Passed to the from-scratch builder (\code{dim_type}, \code{dim_unit},
\code{array_name}, etc.) and ignored on the template path.}
}
\value{
Path to the VRT (invisibly) or the XML string if \code{output} is \code{NULL}.
}
\description{
\code{vrtstack()} builds a GDAL multidimensional VRT that concatenates a set of
sources along one axis. Two construction paths share a single concat-axis
resolver:
}
\details{
\itemize{
\item \strong{from-scratch} (\code{template = NULL}) — the original GeoTIFF/COG path. Each
2D source contributes one slab of a \emph{single} new 3D array. Sources use the
classic \verb{<SourceBand>} + \verb{<SourceTranspose>} form.
\item \strong{template-extend} (\verb{template = <vrt>} or \code{template = TRUE}) — for
mdim-recognised sources (NetCDF/Zarr/HDF5) that may carry \emph{several} arrays
(OISST: anom/err/ice/sst). A small "first-n" mdim VRT is taken as the
recipe, and the full file set is fleshed out by re-parameterising only the
concat dimension and the per-array \verb{<Source>} lists. Everything else
(coordinate arrays, slab geometry, scale/offset/unit/attributes) is carried
verbatim from the template.
}

The template is the GDAL-native analogue of the "recipe-not-payload"
principle: GDAL's own \verb{gdal mdim mosaic} writes the recipe for the first n
files; we only template the axis that varies with the file set.
}
\examples{
#vrt <- vrtstack(<files>, concat = "(\\d{8})", parse_format = "\%Y\%m\%d",
#origin = "1978-01-01", unit = "days",   # must match the template
#template = TRUE, concat_dim = "time")

}
