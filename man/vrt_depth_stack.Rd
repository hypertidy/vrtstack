% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{vrt_depth_stack}
\alias{vrt_depth_stack}
\title{Stack Rasters by Depth/Level into Multidimensional VRT}
\usage{
vrt_depth_stack(
  files,
  values,
  direction = "DOWN",
  unit = NULL,
  dim_name = NULL,
  array_name = NULL,
  output = NULL
)
}
\arguments{
\item{files}{Character vector of file paths}

\item{values}{Numeric vector of depth/level values, or regex pattern to extract}

\item{direction}{Vertical direction: \code{"DOWN"} (depth increases), \code{"UP"} (elevation).
Default: \code{"DOWN"}}

\item{unit}{Unit string (e.g., \code{"m"}, \code{"cm"}, \code{"hPa"})}

\item{dim_name}{Dimension name. Default: \code{"depth"} if direction is DOWN,
\code{"level"} if UP.}

\item{array_name}{Name for the data array}

\item{output}{Output VRT path}
}
\value{
Path to VRT file (invisibly), or XML string if \code{output} is NULL.
}
\description{
A convenience wrapper for \code{\link[=gdal_mdim_stack]{gdal_mdim_stack()}} for vertical profile data
(soil layers, atmospheric levels, ocean depths, etc.)
}
\examples{
\dontrun{
# Soil layers at specific depths
vrt_depth_stack(
  files = c("soil_0cm.tif", "soil_30cm.tif", "soil_100cm.tif"),
  values = c(0, 30, 100),
  unit = "cm",
  output = "soil_profile.vrt"
)

# Extract depth from filename
vrt_depth_stack(
  files = list.files(pattern = "temp_\\\\d+m\\\\.tif$"),
  values = "(\\\\d+)m",  # extracts "50" from "temp_50m.tif"
  unit = "m",
  output = "ocean_profile.vrt"
)
}
}
