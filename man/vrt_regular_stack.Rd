% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{vrt_regular_stack}
\alias{vrt_regular_stack}
\title{Stack Rasters with Regular Spacing}
\usage{
vrt_regular_stack(
  files,
  dim_name,
  start,
  increment,
  unit = NULL,
  array_name = NULL,
  output = NULL
)
}
\arguments{
\item{files}{Character vector of file paths, in order}

\item{dim_name}{Name for the new dimension}

\item{start}{Starting value}

\item{increment}{Spacing between values}

\item{unit}{Unit string}

\item{array_name}{Name for the data array}

\item{output}{Output VRT path}
}
\value{
Path to VRT file (invisibly), or XML string if \code{output} is NULL.
}
\description{
A convenience wrapper for \code{\link[=gdal_mdim_stack]{gdal_mdim_stack()}} when files represent regularly
spaced values (e.g., monthly data, hourly snapshots, uniform vertical levels).
}
\examples{
\dontrun{
# 12 monthly files
vrt_regular_stack(
  files = sprintf("precip_\%02d.tif", 1:12),
  dim_name = "month",
  start = 1,
  increment = 1,
  output = "monthly_precip.vrt"
)

# Pressure levels at 50 hPa intervals
vrt_regular_stack(
  files = level_files,
  dim_name = "pressure",
  start = 1000,
  increment = -50,
  unit = "hPa",
  output = "pressure_levels.vrt"
)
}
}
